# Project instructions

## Before committing

Always run `yarn prettier:check` before committing. If it reports unformatted
files, run `yarn prettier --write <files>` (or fix them) until the check passes,
then commit. Do not commit code that fails the Prettier check.
